@model IEnumerable<ShopApp.Models.tblStorage>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Storage)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Storage)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.StorageId }) |
            @Html.ActionLink("Details", "Details", new { id=item.StorageId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.StorageId })
        </td>
    </tr>
}

</table>
